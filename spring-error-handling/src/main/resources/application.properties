# server.error.path を設定。(デフォルトは /error）
server.error.path=/globalError

# 404 の場合も例外をスローさせる（NoHandlerFoundException）
spring.mvc.throw-exception-if-no-handler-found=true

# 静的リソースへ（「/**」）のアクセスを解除する。
spring.resources.add-mappings=false
